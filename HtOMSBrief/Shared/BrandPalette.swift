import SwiftUI

/// 앱과 위젯이 함께 사용하는 한통도시락 그레이·오렌지 브랜드 색상.
enum BrandPalette {
    static let background = Color(red: 0.055, green: 0.055, blue: 0.060)
    static let card = Color(red: 0.155, green: 0.155, blue: 0.165)
    static let boardCell = Color(red: 0.095, green: 0.095, blue: 0.105)
    static let orange = Color(red: 0.95, green: 0.39, blue: 0.21)
    static let mutedText = Color(red: 0.68, green: 0.68, blue: 0.70)
}
